{% extends 'base.html.twig' %}

{% block body %}

    <h1>{{ wish.title }}</h1>
    <p><strong>Description:</strong> {{ wish.description }}</p>
    <p><strong>Author:</strong> {{ wish.author }}</p>
    <p><strong>Published:</strong> {{ wish.published ? 'Yes' : 'No' }}</p>
    <p><strong>Date Created:</strong> {{ wish.dateCreated|date('Y-m-d') }}</p>
    <p><strong>Category:</strong> {{ wish.categories.name }}</p>
    <a href="{{ path('wish_list') }}">Retour à la liste</a>

{% endblock %}
